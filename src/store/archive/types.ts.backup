import { InputState } from './inputs/types'
import { SnapState, SnapType } from './snaps/types'
import { UnitAssignmentState } from './units/types'

export interface RootState {
  units: 'metric' | 'standard'
  snapType: SnapType
  snaps?: SnapState
  inputs?: InputState
  results: string
  unitAssignment?: UnitAssignmentState
}
